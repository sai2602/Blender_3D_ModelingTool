1. specify the location of bin (.obj file), the object (.stl file) and the location where the images are to be saved in the variables 'bin_loc_base', 'obj_loc_base' and 'img_loc_base' respectively
2. change the number of objects to be added with the variable 'num_of_objs'
3. Copy the code in the scripting tab of the scene and run script